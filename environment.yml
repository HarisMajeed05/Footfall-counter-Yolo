name: footfall-detection
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - ffmpeg
  - pip:
      - torch==2.3.1
      - torchvision==0.18.1
      - numpy<2
      - ultralytics>=8.2.0
      - opencv-python-headless>=4.9.0
      - lap>=0.4.0
